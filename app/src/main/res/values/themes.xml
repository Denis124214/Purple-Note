<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyPetTodolist" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowNoTitle">true</item> (в themes)
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.MyPetTodolist" parent="Base.Theme.MyPetTodolist" />
</resources>